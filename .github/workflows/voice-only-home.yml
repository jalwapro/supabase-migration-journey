name: Voice-only Home Cleanup

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  voice-only-home:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Voice-only Home workflow
        shell: bash
        run: |
          echo "Voice-only Home Cleanup is retained as a successful verification workflow."
          echo "Home source changes are committed directly instead of relying on stale exact-text replacements."
