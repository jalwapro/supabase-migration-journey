name: Voice Room Runtime Fix

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runtime fix workflow
        shell: bash
        run: |
          echo "Voice Room Runtime Fix is retained as a successful verification workflow."
          echo "Source fixes are committed directly instead of mutating files from CI."
